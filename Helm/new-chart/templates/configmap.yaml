apiVersion: v1
kind: ConfigMap
metadata:
  name: devops-project-backend-config
  namespace: {{ .Release.Namespace }}

data:
  PORT: "5000"
  MONGODB_URI: "mongodb://{{ .Values.mongodb.serviceName }}:27017/wanderlust"
