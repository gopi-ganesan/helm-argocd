apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.grafana.name }}
  namespace: {{ .Values.grafana.namespace }}
  annotations:
    alb.ingress.kubernetes.io/scheme: {{ .Values.grafana.annotations.scheme }}
    alb.ingress.kubernetes.io/target-type: {{ .Values.grafana.annotations.targetType }}
spec:
  ingressClassName: alb
  rules:
    - http:
        paths:
         - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.grafana.serviceName }}
                port:
                  number: {{ .Values.grafana.port }}